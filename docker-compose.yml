version: '3.8'

services:
  bot:
    build: .
    container_name: mangabuff-bot
    restart: unless-stopped
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - MAX_CONCURRENT=${MAX_CONCURRENT:-10}
    volumes:
      - bot_data:/app/data

volumes:
  bot_data:
